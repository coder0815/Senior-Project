using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class WaveTimer : MonoBehaviour {

	// Use this for initialization
	void Start () {
        gameObject.GetComponent<Text>().text = "Next Wave";
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
